{
  "name": "griot-urban-culture-backend",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^126.0.0",  // ← Version stable (correction de l'erreur)
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.3.5",
    "nodemailer": "^6.9.12",
    "express-rate-limit": "^6.7.0",  // Ajout pour la sécurité
    "helmet": "^7.1.0",              // Ajout pour la sécurité
    "morgan": "^1.10.0"              // Ajout pour les logs
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
